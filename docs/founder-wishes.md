# Founder Wishes & Decisions — lookup (living)

> A running collection of Marcus's stated preferences and decisions, so he can
> check his own intent when unsure. Maintained by KOMPASS. Not canonical until
> he ratifies. Last updated: 2026-07-20.
>
> How to use: when unsure "did we decide X?", check here first. Entries are
> dated. "Open / to confirm" at the bottom lists things not yet locked.

## Funding & legal
- Kickstart = Kickstarter campaign + a small, tiered membership fee
  (member-owned capital). Funded by members, NOT by parties or capture
  channels. (2026-07-20; see layers-and-funding.md §6.3)
- Party-system funding is rejected: it contradicts the founding premise and
  kills the cross-partisan neutrality that is the product. (2026-07-20)
- A Bürgerstiftung may be the neutral holding entity; EU grants (NGI/Horizon)
  are a later scale option, not the kickstart. (2026-07-20)

## Role & framing of the platform
- Gegenpol is the CONNECTOR, not the REPLACER: it hosts and connects
  Vereine / Genossenschaften / civil society, it does NOT become the
  organization or a political actor. (2026-07-20)
- Competes on story, trust, connection — not fact-checking alone, never rage.
- North Star = direction held without arrival (CONSTRAINT Z). No utopia / fixed
  goal as an endpoint.
- DETECT-not-ENFORCE: make unfairness / manipulation visible, never force or
  coerce a remedy. (standing)

## Council & agents
- Council-of-agents = a plaything run WITH Marcus (agents as stand-in "humans"
  because he's solo); NOT an autonomous off-mode worker. (2026-07-19)
- World tier = align / translate between countries only; no steer, no decide,
  no world-government function. (2026-07-19, founder catch)
- Off-mode principle: weak model SURFACES, strong model + Marcus DECIDE.
  (standing)
- MULTI-PROVIDER CODING via OpenCode (future setup, founder confirmed 2026-07-20):
  OpenCode is the coding worker; provider-agnostic. Marcus has an OpenCode Go
  Abonnement for programming tasks → use it for app code, NOT for Gegenpol design
  (KOMPASS/Nous handles design). Setup: `npm i -g opencode-ai` (or brew);
  `opencode auth login` (or set Abonnement key as OPENROUTER_API_KEY / provider
  env); then smoke test `opencode run 'Respond with exactly: OPENCODE_SMOKE_OK'`.
  Parallel + different-provider runs supported via `--model <provider/model>` and
  separate workdirs. KOMPASS orchestrates; OpenCode writes the code; verify result
  explicitly (KOMPASS does not fake "I built it"). Reminder for next working session.
- DISCORD MULTI-CHANNEL PROJECT SEPARATION (future, rides on F2 gateway setup;
  founder confirmed 2026-07-20): wire Hermes to Discord; give each project its OWN
  channel/thread so contexts don't mix (project A = #project-a, project B =
  #project-b; Gegenpol stays in the DM). At setup, map each channel to its project
  context. Threads-per-channel = lightweight isolation; separate profiles = hard
  isolation. Open decision (F2 fork): Nous Portal vs self-hosted gateway. Bundle
  with the OpenCode setup reminder above.

## Working style with KOMPASS
- Wants honest, constructive pushback ("roast me, never hold back") — flag power
  concentration and self-contradiction plainly, no hedging.
- Spelling / typos: flag proactively (helps him improve). Distinguish a typo
  (no meaning change) from concept-drift (destination-word slip).
- When a preference is already recorded here, just apply it — no need to
  re-argue or re-justify each time. (2026-07-20)
- Don't cite the meta-instruction when applying a preference (e.g. don't say
  "per your standing instruction" / "you wanted this flagged") — just do it.
  (2026-07-20)

## Timeline & scope
- RETURN TO GERMANY: end of May 2027 (31.5.2027; from Sweden). Hard planning anchor for the
  party mobilization — §2 Abs.3 PartG blocks the founding act (Gründungsversammlung /
  Gate B / Bundeswahlleiterin notification) until Germany-resident. Prep (D-series
  templates, Germany-core recruitment) is expat-safe now. Set D-series refresh cadence
  against this date. (2026-07-19)
- MULTI-COUNTRY: the Germany package must be built as a FORKABLE TEMPLATE, not a
  one-off — similar institutions will be needed per country. Shared block (bearings
  N1–N4, council engine, G29 bridge, G28 sensor, pace/substance firewall) fork-once;
  local block (legal form, founding act, Gate B organ, language, name, pace) per
  country. DE v1 = reference fork. Every country has its own "founder must be resident"
  wall, so replication = recruiting local founders per country (network problem).
  (2026-07-19)

- Member-fee levels (proposed in layers-and-funding.md §6.3) — not yet ratified.
- Pilot Kommune for the first civil-society layer-profile — not chosen.
- Hard line between "host self-organization" and "become a political actor" —
  not yet drawn (layers-and-funding.md T-L5).
- The earlier fee-levels discussion Marcus remembers ("like we talked before")
  is NOT on record in docs/sessions — proposed levels are a fresh proposal to
  confirm, not a restatement.
